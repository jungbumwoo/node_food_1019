extends layouts/main
include mixins/foodBlock

block content
    .foods
        each item in foods
            +foodBlock({
                id:item.id,
                title:item.title,
                views:item.views,
                fileUrl:item.fileUrl
            })
